import React from 'react';
import {StyledSpinner} from './Spinner.styles';

function Spinner({color, fontSize}) {
	return <StyledSpinner color={color} fontSize={fontSize} />;
}

export default Spinner;
